import type { RequestHandler } from '@builder.io/qwik-city';

// Main plugin - analytics moved to plugin@umami.ts
export const onRequest: RequestHandler = () => {
  // Placeholder for any future middleware logic
};